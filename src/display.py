from sense_hat import SenseHat

    def displayRise(senseName):
        senseName.load_image('file path here')

    def displayDrop(senseName):
        senseName.load_image('file path here')

    def displayClouds(senseName):
        senseName.load_image('file path here')

    def displaySun(senseName):
        sensename.load_image('file path here')
